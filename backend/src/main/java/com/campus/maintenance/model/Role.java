package com.campus.maintenance.model;

/**
 * Deprecated placeholder. Role enum lives in {@code com.campus.maintenance.entity.User.Role}.
 */
@Deprecated
public enum Role {
    STUDENT,
    MAINTENANCE,
    ADMIN
}
