package com.campus.maintenance.config;

/**
 * Deprecated placeholder kept to avoid breaking imports from earlier branches.
 * Active security configuration is in {@code com.campus.maintenance.security.SecurityConfig}.
 */
@Deprecated
public final class SecurityConfig {
    private SecurityConfig() {
    }
}
